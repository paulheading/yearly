---
import { InvisibleButton } from "paully";
import { button } from "~scripts/selectors/classNames";
---

<InvisibleButton className={button.back}>
  <u><slot /></u>
</InvisibleButton>

<style lang="scss">
  @use "~mixins" as *;

  .back_button {
    padding: 0.25rem;

    @include focus {
      background-color: black;
      color: white;
    }
  }
</style>
