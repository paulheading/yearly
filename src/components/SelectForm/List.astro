---
import SelectFormItem from "~components/SelectForm/Item";
import cnames from "~scripts/selectors/cnames";

let { options } = Astro.props;

let props = {
  class: cnames.selectForm.list,
  role: "listbox",
};
---

<ul {...props}>
  {
    options &&
      options.map(function ({ title, data }, index) {
        let props = {
          data,
          index,
        };
        return <SelectFormItem {...props}>{title}</SelectFormItem>;
      })
  }
</ul>

<style lang="scss" is:global>
  @use "~mixins" as *;

  $background-color-select: white;

  $border-radius-select: toRem(8);

  .select-form-list {
    border-bottom-right-radius: $border-radius-select;
    border-bottom-left-radius: $border-radius-select;
    background-color: $background-color-select;
    border-bottom: 1px solid currentColor;
    border-right: 1px solid currentColor;
    border-left: 1px solid currentColor;
    max-height: toRem(158);
    position: absolute;
    overflow-y: auto;
    text-align: left;
    list-style: none;
    padding-left: 0;
    display: none;
    width: 100%;
    z-index: 2;
  }
</style>
