---
import { Loaded, Loader, NotLoaded, Wrap } from "paully";
import Layout from "#layouts/Main";
import ChooseCardSection from "#sections/ChooseCard";

import { seo } from "paully/data";

let layoutProps = {
  title: "Build | Yearly | Songs you discovered this year",
  keywords: seo.keywords("yearly, spotify, songs, discover"),
  description: "I'm a " + seo.job.title + ". " + seo.job.description,
};
---

<script src="#pages/build"></script>

<Layout {...layoutProps}>
  <Wrap style={{ textAlign: "center" }}>
    <NotLoaded>
      <Loader style={{ margin: "auto" }} />
    </NotLoaded>
    <Loaded>
      <ChooseCardSection />
    </Loaded>
  </Wrap>
</Layout>
